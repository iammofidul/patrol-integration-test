name: Flutter Android with Patrol Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: macos-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Setup Flutter (specific version with caching)
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.5'
          channel: 'stable'
          cache: true

      # Step 3: Install Android Command-Line Tools
      - name: Install Android Command-Line Tools
        run: |
          echo "Downloading Android Command-Line Tools"
          mkdir -p "$HOME/android-sdk/cmdline-tools"
          curl -o sdk-tools.zip https://dl.google.com/android/repository/commandlinetools-mac-9477386_latest.zip
          unzip -q sdk-tools.zip -d "$HOME/android-sdk/cmdline-tools"
          mv "$HOME/android-sdk/cmdline-tools/cmdline-tools" "$HOME/android-sdk/cmdline-tools/tools"
          rm sdk-tools.zip
          echo "Setting ANDROID_HOME"
          echo "export ANDROID_HOME=$HOME/android-sdk" >> $GITHUB_ENV
          echo "export PATH=$PATH:$ANDROID_HOME/cmdline-tools/tools/bin:$ANDROID_HOME/platform-tools" >> $GITHUB_ENV
        shell: bash

      # Step 4: Install Android SDK and Emulator
      - name: Setup Android SDK and Emulator
        run: |
          echo "Installing Android SDK tools"
          yes | sdkmanager --install "platform-tools" "platforms;android-33" "build-tools;33.0.0"
          echo "Installing Android Emulator"
          yes | sdkmanager --install "emulator" "system-images;android-33;google_apis;x86_64"

      # Step 5: Create and Start Emulator
      - name: Create and Start Emulator
        run: |
          echo "Creating AVD"
          echo "no" | avdmanager create avd -n test -k "system-images;android-33;google_apis;x86_64"
          echo "Starting Emulator"
          nohup emulator -avd test -no-window -no-audio -no-snapshot -gpu swiftshader &

      # Step 6: Wait for Emulator to Boot
      - name: Wait for Emulator to Boot
        run: adb wait-for-device shell 'while [[ -z $(getprop sys.boot_completed) ]]; do sleep 1; done;'

      # Step 7: Run Patrol Test
      - name: Run Patrol Tests
        run: |
          export PATH="$PATH":"$HOME/.pub-cache/bin"
          patrol test -t integration_test/app_test.dart --target-platform android
